# 作業者向け入口
まず START_HERE.md、README.md、docs/03_実装計画.md、reports/検証結果.md を読む。
説明・コメント・利用者画面は日本語を基本にし、IDやコード識別子は英語でよい。

この配布物からGitHubへの変更はまだない。ユーザーの最新指示を確認してから公開する。
既存ファイルやLibraryの成果物を調べずに新しい正本を決めない。

権限は `SearchService` / `Policy` に集約。UIで隠すだけ、検索後にLLMへ制御を任せるだけの方式に変えない。
`demo` の役割切替を本番で有効にしない。実データ・秘密はexamplesやreportsへ置かない。
Chatworkの最新100件を全履歴扱いしない。IDなし履歴を本文ハッシュで完全同定したと主張しない。

変更後は pytest、HTTP試験、公開前チェックを実行する。UI変更は実画面を確認する。
外部接続を検証できなければ、代替試験と実接続を区別して記録する。
