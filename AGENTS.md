# 開発・引き継ぎ

目的は「許可された過去の経緯を、最新の発言とつなげて根拠付きで探せる」体験を作ること。規則・検査・書類を増やすこと自体を目的にしない。

最初にREADME、docs/PLAN.md、docs/FILEMAP.md、docs/VERIFICATION.mdを読む。既存コードの状態を確認し、過去の会話や未確認の記憶を正本にしない。

変更前に目的・範囲・完了条件を更新する。変更後は実際に起動・テストし、機能完了と外部接続の未確認を区別する。画面変更はブラウザで操作・撮影して確認する。動くことと「本番で安全」は別である。

日本語の説明を基本とする。実コードのファイル名・公開API名と文書を一致させる。公式仕様に反する完全同期、未導入のベクトルDB、疑似Embeddingを実装済みとして書かない。

顧客のコード・CSV・秘密・実DB・モデル重みを公開リポジトリへ入れない。公開デモは合成データ専用。実データには別DBとgateway／embeddedを使う。閲覧認可をデモUIやLLM判断へ移さない。

実行する検証：pytest、compileall、公式MCP往復、ブラウザsmoke。DB・モデル変更ではPostgreSQL／実モデル検証を追加する。CI証跡と対象コミットを記録し、未実施を成功としない。

教訓はdocs/LESSONS.mdへ、根拠・適用範囲とともに残す。前提が古くなったら更新・削除する。既存mainを読まずに上書きしない。競合時にforce pushで他者の変更を消さない。
