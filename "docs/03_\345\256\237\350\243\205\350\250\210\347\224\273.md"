# 実装計画
作成：2026-09-11。本格実装前に目的・説明・ファイルマップとともに記録。

## P0: 説明・設計
- [x] ユーザーの最終指示：GitHubへ書き込まず、ここに配布物を作る。
- [x] Libraryを検索し、この名称の既存成果物は今回の検索で見つからなかった。
- [x] 目的・範囲・ファイルマップ・計画を先に作成する。

## P1: 共通基盤
- [x] DB、JWT認証、現在参加ルームとの積集合、失敗時拒否。
- [x] デモ限定の本人切替と、統合時の無効化。

## P2: 履歴と最新データ
- [x] CSV検証、原子的取り込み、ID有無の処理、編集・削除、再実行。
- [x] Chatwork REST、100件重複同期、欠損警告、Webhook署名・永続キュー。

## P3: 検索と操作面
- [x] ACL先行検索、期間読取、本文の再認可、RRF検索インターフェース。
- [x] MCP search/fetch、JSON-RPC/Streamable HTTPの試験。
- [x] 日本語画面、架空データ、同期・権限の操作デモ。

## P4: 検証・引継ぎ
- [x] 越権、改ざん、失効、SQL文字列、XSS、二重取得・編集逆転の試験。
- [x] 実画面の操作・撮影。検証できない外部接続を明記。
- [x] README・提案・統合手順・公開前点検・ZIP・ハッシュ作成。

## 完了条件
依存パッケージが利用可能な環境で起動でき、架空データの検索・取り込み・認可・同期デモが一巡する。
既存PoCへの接続口が実装と資料で一致し、未検証事項が隠れていない。
成果物だけから別のWork/Codexで起動と検証を再現できる。

## 環境上の判断
この会話のコンテナではパッケージ取得のDNS解決が失敗した。既存のFastAPI/SQLAlchemy/PyJWTで実行する。
MCPは公開仕様に沿った小さな読取専用HTTP実装をテストする。公式SDK採用済みとは称さない。
外部Embedding・実Chatwork・GCP・ChatGPT画面の実接続は、テスト用代替と実確認を区別する。

## 完了記録（2026-09-11）
P0〜P4をローカル汎用PoCの範囲で完了。124件の自動試験、16項目の実HTTP試験、12項目の画面試験を実施。
画面試験は実Chromium＋ASGI接続であり、ブラウザHTTP制限と外部サービス未検証を検証結果に明記した。
外部接続の未検証事項を、PoC実装の完了と取り違えない。
配布はZIP・ハッシュ・Work引継ぎ資料。GitHubへの反映はこの作業では行わない。
