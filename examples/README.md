# 付属例の注意
すべて説明用。企業名・発言・利用者・IDは架空です。
`history.csv` と `history_idless.csv` はPoCの正規化形式で、顧客のChatwork管理者CSVではありません。
`chatwork-tokens.example.json` は無効なプレースホルダーで、期限も0です。このファイルを実キーで上書きして公開しないでください。
設定例にあるtenant・subject・account_idは、既存の認証済み本人との対応を確認して登録します。

`mcp-client.py` はデモ専用HTTP検証。統合環境への認証回避用ではありません。
